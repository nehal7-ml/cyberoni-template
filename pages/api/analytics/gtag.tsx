import TagManager, { TagManagerArgs } from "react-gtm-module";

//!
export const GTM_ID = "G-3YKTJT9TGN";

const tagManagerArgs: TagManagerArgs = {
  gtmId: GTM_ID,
};

TagManager.initialize(tagManagerArgs);
